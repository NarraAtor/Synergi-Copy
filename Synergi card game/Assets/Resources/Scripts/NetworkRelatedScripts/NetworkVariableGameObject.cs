using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using System;
using MLAPI.Serialization.Pooled;
using MLAPI.Transports;

/// <summary>
/// Purpose: A network variable that allows me to send game object references across the network.
/// Restrictions:
/// 
/// </summary>
public class NetworkVariableGameObject : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
